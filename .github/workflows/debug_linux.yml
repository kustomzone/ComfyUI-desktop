# .github/workflows/debug_linux.yml
name: Build App Linux Debug

on:
  workflow_dispatch:
  workflow_call:

jobs:
  build-linux:
    runs-on: ubuntu-latest
    steps:
      - name: Blank
        run: echo "Intentionally left blank"
      